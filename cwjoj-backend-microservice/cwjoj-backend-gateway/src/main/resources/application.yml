spring:
  application:
    name: cwjoj-backend-gateway
  main:
    web-application-type: reactive
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      routes:
        - id: cwjoj-backend-user-service
          uri: lb://cwjoj-backend-user-service
          predicates:
            - Path=/api/user/**
        - id: cwjoj-backend-question-service
          uri: lb://cwjoj-backend-question-service
          predicates:
            - Path=/api/question/**
        - id: cwjoj-backend-judge-service
          uri: lb://cwjoj-backend-judge-service
          predicates:
            - Path=/api/judge/**

knife4j:
  gateway:
    # 开启gateway聚合组件
    enabled: true
    # 设置聚合模式采用discover服务发现的模式
    strategy: discover
    discover:
      # 开启discover模式
      enabled: true
      # 聚合子服务全部为Swagger2规范的文档
      version: swagger2
